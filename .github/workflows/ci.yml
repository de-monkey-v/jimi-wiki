name: CI

on:
  pull_request:
  push:
    # develop도 검사한다. main만 걸어두면 ff-merge로 승격한 뒤에야 CI가 처음 돌아,
    # 깨진 변경을 릴리스 브랜치로 올린 다음에 발견하게 된다.
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: jimi
          POSTGRES_PASSWORD: jimi
          POSTGRES_DB: jimi
        ports:
          # 5434 = 개발 DB 포트 계약 — scripts/run-dev-db-command.mjs 가드가 loopback:5434/jimi만 허용
          - 5434:5432
        options: >-
          --health-cmd "pg_isready -U jimi -d jimi"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://jimi:jimi@localhost:5434/jimi?schema=public
      AUTH_SECRET: ci-secret
      GEMINI_API_KEY: ""
      GOOGLE_GENERATIVE_AI_API_KEY: ""
      DEV_USER_EMAIL: dev@jimi.local
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm prisma generate
      - run: pnpm prisma migrate deploy
      - run: pnpm check:rules
      - run: pnpm check:i18n
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm smoke
      - run: pnpm build
